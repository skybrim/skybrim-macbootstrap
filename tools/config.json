{
    "inbounds": [{
        "port": 10009, // SOCKS 代理端口，在浏览器中需配置代理并指向这个端口
        "listen": "127.0.0.1",
        "protocol": "socks",
        "settings": {
            "udp": true
        }
    }],
    "outbounds": [{
        "protocol": "vmess",
        "settings": {
            "vnext": [{
                "address": "34.92.216.14", // 服务器地址，请修改为你自己的服务器 ip 或域名
                "port": 25028, // 服务器端口
                "users": [{
                    "id": "a958f179-e2d5-452b-a942-1bc9b394ee52"
                }]
            }]
        }
    }, {
        "protocol": "freedom",
        "tag": "direct",
        "settings": {}
    }],
    "routing": {
        "domainStrategy": "IPOnDemand",
        "rules": [{
            "type": "field",
            "ip": ["geoip:private"],
            "outboundTag": "direct"
        }]
    }
}